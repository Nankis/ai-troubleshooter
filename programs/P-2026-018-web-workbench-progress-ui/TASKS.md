# TASKS

- [x] 梳理现有 Web Chat 同步流程。
- [x] 增加 `GET /web/api/overview`。
- [x] 增加 `GET /web/api/cases/{case_no}`。
- [x] 支持 `POST /web/api/chat` 的 `async=1` 异步排查。
- [x] 支持 `GET/POST /web/api/knowledge` 和 `DELETE /web/api/knowledge/{id}`。
- [x] Store 增加 recent cases 和知识软删除。
- [x] 重做三栏 Codex 风格 UI。
- [x] 单元测试覆盖异步状态、overview、知识录入/删除。
- [x] 浏览器实际启动和点击验证。
