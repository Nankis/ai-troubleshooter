# HANDOFF

## 当前状态

- 架构边界已在 README 和 ADR 中修正。
- Go baseline 已改为 `decisionbaseline`。
- Program 追加规则已写入 `STANDARDIZATION.md`。
- `program-history-rewrite` 错误已写入 `docs/LESSONS.md`。
- 验证结果写法已写入 `docs/VERIFICATION.md`。

## 后续建议

- 下一步如果接 Python decision-engine 的真实 worker 调用链，应新增独立 Program。
- 如果继续调整目录或命名，也应新增 Program，而不是回写旧 Program。
- 后续开始任务时先读 `AGENTS.md` 和 `docs/LESSONS.md`。
- 写验证结果时按 `docs/VERIFICATION.md` 的 Evidence/Result 模板执行。
