# DECISIONS

## D1: 预览层只消费本地 object URL

放大预览不走后端，不产生临时上传文件，只复用当前待提交 `FileList` 对应的 object URL。

## D2: 移除按钮和放大按钮分离

缩略图主体是放大按钮，右上角按钮只负责移除图片，避免用户想看图时误删。

## D3: 文件列表变化时关闭大图

选择、粘贴或移除图片都会刷新本地 object URL，因此刷新前先关闭大图，防止放大层引用已释放 URL。
