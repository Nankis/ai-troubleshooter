# P-2026-026 Web Independent Pane Scroll

## 背景

Web 工作台左、中、右三块区域内容长度不同。原页面外层仍可滚动，导致左侧工具/经验列表滚动时会带动中间和右侧区域一起移动，体验不接近 Codex。

## 目标

- 左侧区域独立上下滚动。
- 中间对话区独立滚动，顶部标题和底部输入框保持固定。
- 右侧进度区域独立上下滚动。
- 外层页面不产生整体滚动。
- 实际启动 Web 页面验证滚动互不影响。

## 非目标

- 不改排障业务逻辑。
- 不改响应式隐藏规则。
- 不引入前端框架。
