services:
  ai-troubleshooter:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        SERVICE: investigation-gateway
    ports:
      - "8080:8080"
    environment:
      APP_ENV: dev
      HTTP_PORT: 8080
      DB_DRIVER: memory
      CONNECTOR_MODE: mock
